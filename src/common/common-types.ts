export type LedColor = {
  r: number;
  g: number;
  b: number;
};

export type NumericStat = {
  min: number;
  max: number;
  avg: number;
};

export type Point2D = {
  x: number;
  y: number;
};

export type Point3D = {
  x: number;
  y: number;
  z: number;
};
